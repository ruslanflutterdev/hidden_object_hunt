# 🕵️ Hidden Object Hunt

**Hidden Object Hunt** — это увлекательная мобильная игра-головоломка на Flutter. Ваша задача — найти скрытый объект на игровом поле, избегая ловушек. Испытайте свою удачу и логику!

## ✨ Основные возможности

* **🎮 Игровое поле**: Динамическая сетка клеток (Grid), каждая из которых может скрывать сюрприз.
* **💣 Опасные зоны**: Среди клеток спрятаны бомбы — одно неверное нажатие, и игра окончена!
* **⏱️ Управление игрой**:
    * Отслеживание количества ходов или времени (через виджет `GameControls`).
    * Возможность перезапуска игры в любой момент.
* **📱 Адаптивный интерфейс**: Игра подстраивается под экраны различных устройств.
* **🎨 Графические ассеты**: Использование кастомных изображений (например, бомб) для визуализации состояния клетки.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **Архитектура**:
    * Использование `StatefulWidget` для управления состоянием игры (открытые/закрытые клетки, статус победы).
    * Разделение на **Screens** (Экраны) и **Widgets** (Компоненты) для чистоты кода.
* **Ассеты**: Работа с локальными ресурсами изображений (`assets/`).

## 📂 Структура проекта

Проект разбит на логические модули:

```text
lib/
├── models/           # Логика данных
│   └── cell.dart          # Модель клетки (состояние: открыта/закрыта, есть ли бомба)
├── screens/          # Экраны приложения
│   ├── start_screen.dart  # Стартовый экран (Меню)
│   └── game_screen.dart   # Основной экран с игровым полем
├── widgets/          # Переиспользуемые UI компоненты
│   ├── cell_widget.dart   # Виджет одной клетки сетки
│   └── game_controls.dart # Панель управления (счетчик, рестарт)
└── main.dart         # Точка входа в приложение
